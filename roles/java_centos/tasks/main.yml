---
- name: install java
  become: true
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - "{{ java_version }}"
    - "{{ java_version }}-devel"

- name: setup java environment variables
  become: true
  template:
    src: java_env.sh
    dest: /etc/profile.d/java_env.sh