---
- name: install java
  become: true
  yum:
    name: "{{ java_version }}"
    state: present
  yum:
    name: rsync
    state: present

- name: setup java environment variables
  become: true
  template:
    src: java_env.sh
    dest: /etc/profile.d/java_env.sh